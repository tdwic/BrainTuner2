<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.braintuner">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".game_start_page">

          <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".game_menu" />
        <activity android:name=".player_score" >

        </activity>
        <activity android:name=".level_4_a" />
        <activity android:name=".level_4" >


        </activity>
        <activity android:name=".level_3_a" />
        <activity android:name=".level_3" />
        <activity android:name=".level_2_a" />
        <activity android:name=".level_2" />
        <activity android:name=".level_1_a" />
        <activity android:name=".level_1" />
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>